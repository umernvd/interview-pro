org.gradle.jvmargs=-Xmx2g -Xms256m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
org.gradle.daemon=true


# Release Signing Configuration
# For production builds, set these properties in local.properties or environment variables:
# INTERVIEW_PRO_KEYSTORE_PATH=/path/to/keystore.jks
# INTERVIEW_PRO_KEYSTORE_PASSWORD=your_keystore_password
# INTERVIEW_PRO_KEY_ALIAS=your_key_alias
# INTERVIEW_PRO_KEY_PASSWORD=your_key_password
# 
# For development: Leave unset to use debug signing keys
# For CI/CD: Set via environment variables or secure build configuration
